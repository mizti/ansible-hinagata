---
ansible_ssh_private_key_file: ~/.ssh/id_rsa.ansible-user

# bear_env: vagrant
# consul_datacenter: vagrant

region_name: hoge

dns:
  servers:
    - 8.8.8.8
  search:
    - some.com

ntp:
  servers:
    - server 0.ubuntu.pool.ntp.org
    - server 1.ubuntu.pool.ntp.org

